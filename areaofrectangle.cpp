#include <iostream>
using namespace std;

int main()
    {
        {
    
       // int length = 8;
    //int width = 5;
    std::cout << "Enter the length of the rectangle: ";
    std::cin >> length;
    
    std::cout << "Enter the width of the rectangle: ";
    std::cin >> width;
    
    
    //  calculate the length and width of the rectangles 
    int area = length * width;
    
    //  then Output the area
    std::cout << "then the area of the rectangle is: " << area << std::endl;


    return 0;
}
    }
    

